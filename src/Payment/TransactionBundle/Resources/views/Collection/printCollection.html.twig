<script type="text/javascript">
function printService()
{
	document.getElementById("print").style.visibility='hidden' ;
	window.print();
	document.getElementById("print").style.visibility='visible';
}

window.onbeforeunload = confirmExit;
function confirmExit()
{
  return "Seguro que desea salir de esta pagina, por favor verifiue si ya imprimio el comprobante de pago";
}
</script>
<link rel="stylesheet" type="text/css" href="{{ asset('css/modules.css') }}" />
<table  align="right" id="print">
  <tr>
    <td><a href='javascript:printService();' ><img src="{{ asset('images/printer.png') }}" border="0" width="12px">Imprimir</a></td>
     <td>&nbsp;<a href='javascript:window.close();' ><img src="{{ asset('images/cancel.png') }}" border="0" width="10px">Cerrar</a></td>
  </tr>
</table>
<table id="printTable"  class="printReport">
<tr><td height="55px">&nbsp;</td></tr>
<tr><td>{{ account.member.name }} {{ account.member.lastName }}</td><td width="28%">{{ account.meterNumber }}</td><td width="15%"><font size="3"><b>{{ account.accountNumber }}</b></font></td></tr>
<tr><td>{{ account.member.documentNumber }}</td><td colspan="2">{{ dateFac }}</td></tr>
<tr><td>{{ account.member.address }}</td><td colspan="2">{{ dateImp }}</td></tr>
</table>
<br>
{% if(contItems > 0) %}
<table id="printTableDetail" border="1"  class="printReport">
<tr><td height="10px">&nbsp;</td></tr>
{% set total = 0 %}
{% for item in items %}
	<tr>
	{% set ant = '' %}
	{% set act = '' %}
	{% set con = '' %}
	{% if(item.type == 1) %}
		{% set ant = item.entity.meterPreviousreading.meterCurrentReading %}
		{% set act = item.entity.meterCurrentReading %}
		{% set con = act - ant %}
	{% endif %}
	<td class="itemDetail" width="10%">{{ ant }}</td>
	<td class="itemDetail" width="10%">{{ act }}</td>
	<td class="itemDetail" width="10%">{{ con }}</td>
	<td> {{ item.motive }}</td>
	<td class="itemDetail" width="5%"> {{ item.amount }} {% set total = total + item.cost %}</td>
	<td class="itemDetail" width="10%"> {{ item.unitCost | number_format(2) }}</td>
	<td class="itemDetail" width="10%"> {{ item.cost  | number_format(2) }}</td>
	</tr>
{% endfor %}
<tr><td colspan="6" align="right">&nbsp;</td><td class="itemDetail"><b>{{ total | number_format(2) }}</b></td></tr>
</table>
<br>
Válido como comprobante de Pago si tiene el sello de cancelación.
<br><br><br>

{% else %}
No existe consumos para esta cuenta.
{% endif %}